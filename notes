on MAC: to activate iron_volunteering virtual environment: 
--> source iron_volunteering/bin/activate
If both the venv (iron_volunteering) and (base) are running:
--> conda deactivate

on WINDOWS: iron_volunteering\Scripts\activate

To install the requirements (must be in iron_volunteering folder directory): 
- pip install -r requirements.txt

to run application: 
--> streamlit run main.py